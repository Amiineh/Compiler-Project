int add(int a, int b){    int c;    c =a+   b;    return c;}int Add(int a, int b){    int c[10];    int y;    int sum;    c[0] = a;    c[1] = b;    y = 0;    sum = 0;    while (y < 1)        if (y == 0)            sum = sum+c[y];        else            sum=sum -c[y];    y = 0;    while (y < 1)        if (y == 0)            sum = sum*c[y];        else            sum =sum+c[y];    return sum+45;}void main(void) {    int x;    x = Add(add(2, 4), 3);}EOF